<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blackjack</title>
    <link rel="stylesheet" href="css/blackstyle.css">
</head>
<body>
    <script>
        var suits = ["Spades", "Hearts", "Diamonds", "Clubs"];
        var values = ["Ace", "2", "3", "4", "5", "6", "7", "8", "9", "10", "Jack", "Queen", "King"]
        var deck = new Array();

        //this will create the deck of card from the var above
        function CreateDeck()
        {
            deck = new Array();
            for (var i = 0; i < values.length; i++)
            {
                for(var x = 0; x < suits.length; x++)
                {
                    var amount = parseInt(values[i]);
                    if (values[i] == "Jack" || values[i] == "Queen" || values[i] == "King")
                        amount = 10;
                    if (values[i] == "Ace")
                        amount = 11;
                    var card = {Value: values[i], Suit: suits[x], Amount: amount};
                    deck.push(card);
                }
            }
        }

        //this will shuffle the deck of cards
        function Shuffle()
        {
            for(var i = 0; i < 1000; i++)
            {
                var location1 = Math.floor((Math.random() * deck.length));
                var location2 = Math.floor((Math.random() * deck.length));
                var temp = deck[location1];

                deck[location1] = deck[location2]
                deck[location2] = temp;
            }
        }

        var player
    </script>
    <embed src="" type="">
    <div class="container">
        <h1 class="text-center">Welcome to Blackjack, (Users Name)</h1>
    </div>
    <div class="blackjackbody">
        <img src="https://cdn2.vectorstock.com/i/1000x1000/07/91/blackjack-table-vector-6320791.jpg" alt="">
    </div>
    <div class="buttons text-center">
        <a href="" class="btn btn-secondary ml-5 mr-5" onclick="stay()">Stay</a>
        <a href="" class="btn btn-secondary ml-2 mr-5" onclick="DealCards()">Deal</a>
        <a href="" class="btn btn-secondary mr-5" onclick="Hit()">Hit</a>
    </div>
</body>
</html> 